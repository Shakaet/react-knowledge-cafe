import React from 'react';

const Pass = ({pass}) => {

    

    return (
        <div>
            <h2 className='text-2xl'>{pass.title}</h2>
            
        </div>
    );
};

export default Pass;